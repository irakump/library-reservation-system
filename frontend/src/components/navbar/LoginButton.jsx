import React from 'react'

const LoginButton = () => {
    // TODO: Show "log in" if user is not logged in
  
    return (
    <div>
      <button>Log Out</button>
    </div>
  )
}

export default LoginButton
